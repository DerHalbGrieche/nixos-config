stages:
  - mirror

mirror_to_github:
  stage: mirror
  script:
    - git remote add github https://oauth2:${GITHUB_TOKEN}@github.com/DerHalbGrieche/nixos-config.git
    - git push --mirror github
  only:
    - main  # Optional: Passe dies an, wenn andere Branches gespiegelt werden sollen

